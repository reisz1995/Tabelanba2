name: OpenCode Sync

on:
  push:
    branches: [ "main" ]

jobs:
  opencode:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run OpenCode
        run: |
          npm install
          npm run opencode
